# Kandy PM2.5 additive-decomposition model — runtime dependencies
# (the model runs fully locally; no GPU / PyTorch / Kaggle needed)
#
# Versions are floored at the releases this model was built and validated with
# (Python 3.11) and capped below the next major where that ecosystem has had
# breaking changes (numpy 2, osmnx 2, geopandas 1). Loosen if you know better.

# core numerics + IO
numpy>=2.4,<3
pandas>=2.3,<3
pyarrow>=23.0            # parquet engine for pandas
scipy>=1.17,<2
xarray>=2026.2

# modelling
lightgbm>=4.6,<5         # lag-free T(t) temporal anchor
networkx>=3.6,<4         # road-network centrality (traffic emission source)

# plotting
matplotlib>=3.10,<4
scienceplots>=2.2
cmocean>=4.0

# geospatial (figures + terrain/OSM)
osmnx>=2.1,<3            # OSM roads/river/places for the study-area basemap
geopandas>=1.1,<2
shapely>=2.1,<3
pyproj>=3.7,<4
rasterio>=1.4,<2        # DEM / GeoTIFF IO
cartopy>=0.25           # locator inset + graticule
contextily>=1.7         # basemap tiles (F1)
